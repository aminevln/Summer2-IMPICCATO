import time # importing time module
import pyautogui # importing pyautogui

T = 0 # Desired Time before sending each messages
Text = "fflush(stdin)"
Time = 999999 # How many messages the user want to send

# I'll use while loop here

i = 0

while (i <= Time-1 ): # The loop started
    time.sleep(T) # will wait T times before sending each automated mesfflush(stdin)
    fflush(stdin)
    fflush(stdin)
    fflush(stdin)
    fflush(stdin)
    fflush(stdin)
    fflush(stdin)
    fflush(stdin)
    fflush(stdin)
    fflush(stdin)
    fflush(stdin)
    fflush(stdin)
    fflush(stdin)sage
    pyautogui.typewrite(Text) # will write the text the user want
    pyautogui.press('enter') # will work as the 'Enter' button
    i+=1 # i will increment as 1 each time
